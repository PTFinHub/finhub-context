# Auto detect text files and perform LF normalization
* text=auto eol=lf

# Markdown sempre LF — PT-PT + CRLF gera diffs falsos de centenas de linhas
*.md text eol=lf
*.txt text eol=lf
*.json text eol=lf
